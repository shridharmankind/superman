import {StyleSheet} from 'react-native';
import theme from 'themes';

const styles = StyleSheet.create({
  container: {
    marginVertical: theme.spacing(21.3),
    paddingHorizontal: theme.spacing(2),
  },
});

export default styles;
